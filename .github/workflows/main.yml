name: Build Flutter APK

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.7'
          channel: 'stable'

      - name: Install dependencies
        working-directory: front-end
        run: flutter pub get
        
      - name: Print SHA1
        working-directory: front-end
        run: keytool -list -v -keystore android/app/release.keystore -alias release -storepass ${{ secrets.KEYSTORE_PASSWORD }}
  
      - name: Run tests
        working-directory: front-end
        run: flutter test

      - name: Build APK (release)
        working-directory: front-end
        run: flutter build apk --release

      - name: Create build info file with Swagger link
        run: |
          echo "Build date: $(date)" > build_info.txt
          echo "Swagger documentation: https://epiflipboard-iau1.onrender.com/docs" >> build_info.txt
          echo "Commit: $GITHUB_SHA" >> build_info.txt

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: flutter-apk
          path: front-end/build/app/outputs/flutter-apk/app-release.apk

      - name: Upload build info artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-info
          path: build_info.txt
